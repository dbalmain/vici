==============================================================
 vici — every bound feature, one line at a time
==============================================================

 Open this in the harness:   cargo run -p vici-harness

 Harness keys (these bypass the editor entirely):
   F10    quit, always
   F2     hide/show the effect log
   <C-c>  quit, from normal mode only
   :w     save        :q  quit      :q!  discard     :wq  both

 Tick a box by putting the cursor on the space and typing  rx
 — which is itself a test of `r`. Then `u` to untick it.

 The right pane logs every effect the core emits. Watch it: one
 `edit` per keystroke while typing is the thing tree-sitter needs,
 and it is easy to get wrong.

 Nothing here is precious. Break it, then `u` your way back.

--------------------------------------------------------------
 1. MODES
--------------------------------------------------------------

[x] i   insert before the cursor
[x] a   insert after the cursor
[x] I   insert at the first non-blank
[x] A   insert at end of row
[x] o   open a row below
[x] O   open a row above
      indent me and press I, then A, then o
[x] R   replace mode, overwrite as you type
      overwrite the middle of this line
[x] v   visual by character
[x] V   visual by line
[x] <Esc> back to normal from any of the above

    Check: after `iabc<Esc>` the cursor sits on `c`, not past it.
    Check: the cursor is a block in normal, a bar in insert.

--------------------------------------------------------------
 2. MOTIONS — characters and rows
--------------------------------------------------------------

[x] h  left            [x] l  right
[x] j  down            [x] k  up
[x] <Left> <Right> <Up> <Down>   same, as arrows
[x] <Space>  right
[x] 0  first column        [x] <Home>
[x] ^  first non-blank     (leading spaces below)
[x] $  last column         [x] <End>
      	tabbed and indented, try 0 then ^
[x] gg  first row
[x] G   last row

    Sticky column: put the cursor at the end of the long row,
    then press j twice. It should return to the end each time.
      a very long row indeed, ending here
      short
      another quite long row, ending here

--------------------------------------------------------------
 3. MOTIONS — words
--------------------------------------------------------------

      alpha beta.gamma delta_eps  ninety9 end
[x] w   next word start
[x] W   next WORD start   (ignores the `.`)
[x] b   previous word start
[x] B   previous WORD start
[x] e   end of word
[x] E   end of WORD

--------------------------------------------------------------
 4. MOTIONS — find on the row
--------------------------------------------------------------

      select id, name, email from users
[x] f,  forward to the comma
[x] t,  forward, stopping before it
[x] F,  backward to a comma
[x] T,  backward, stopping after it
[ ] ;   repeat the last find
[ ] ,   repeat it in reverse

--------------------------------------------------------------
 5. COUNTS
--------------------------------------------------------------

[x] 3j  down three rows
[x] 5l  right five
[x] 3x  delete three characters
[x] 2dw delete two words
[x] 2d3w delete six words — counts multiply
[x] 5G  go to row five
[x] 1G  row one (and bare G is the last row — not the same)
[x] 0   is a motion, not a count: `d0` deletes to the start
      one two three four five six seven eight nine ten

    Watch `cmd:` in the status bar while you type a count.

--------------------------------------------------------------
 6. OPERATORS over motions
--------------------------------------------------------------

      delete from here to somewhere over there
[x] dw  delete word           [x] de  delete to word end
[x] d$  to end of row         [x] d0  to start of row
[x] dj  delete two rows — a linewise motion
[x] dG  to the last row       [x] dgg to the first
[x] dt, delete up to a comma, keeping it
[x] cw  change word — behaves like `ce`, no trailing space
[x] ce  change to word end
[x] c$  change to end of row
[x] yw  yank a word, then `p` to paste it
[x] y$  yank to end of row

    `cw` on the last word of a row is the classic irregularity:
    it must not swallow the newline.

    An exclusive motion reaches one past the last character, so on
    the very last word of the file `dw` takes all of it. Go to the
    end with G$ and check — this used to leave one character.

 Case, over the same motions and objects:
[ ] gUw  upper-case a word     [ ] guw  lower-case it
[ ] g~w  swap case over a word
[ ] 2gUw  two words            [ ] gU2w  the count either side
[ ] gUiw  over a text object   [ ] gUi(  over a parens block
[ ] gUt,  over a find
      make this shout, then quieten it down again
[ ] gUU  this whole row        [ ] gUgU  the long form
[ ] guu  [ ] g~~  the same for lower and swap
[ ] 2guu  this row and the next

    Case changes do not touch the register: yank something, then
    gUW, then p — you should get what you yanked. Watch reg[] in
    the status bar stay put.

    gUiw on `straße` gives STRASSE — one character becoming two is
    why these go through the full Unicode mapping.

--------------------------------------------------------------
 7. DOUBLED OPERATORS — whole rows
--------------------------------------------------------------

[x] dd  delete this row
[x] cc  change this row, keeping the row itself
[x] yy  yank this row, then `p` below / `P` above
[x] 3dd delete three rows
      row one of four
      row two of four
      row three of four
      row four of four

    `dd` on the very last row of the file takes the *preceding*
    newline. Scroll to the bottom and try it.

--------------------------------------------------------------
 7A. SHIFT — host-configured indentation
--------------------------------------------------------------

[ ] >>  shift this row right       [ ] <<  shift it left
[ ] 3>> shift this row and two more
[ ] >j  shift this row and the next
[ ] >ip shift a paragraph          [ ] >i{ shift a braces block
[ ] Vj> shift a visual line selection
[ ] >> then .  repeat the shift
      one row to indent
      second row to indent

    Watch the effect log: one Edit per changed row, but one `u`
    restores the whole shift. Empty rows stay empty; whitespace-only
    rows do move. `>w` still shifts its whole row, because shift is
    always linewise. The host's Indent configuration chooses shift and
    tab widths, plus whether new indents use tabs.

--------------------------------------------------------------
 8. TEXT OBJECTS
--------------------------------------------------------------

      the quick brown fox jumps
[x] diw  inner word          [x] daw  a word, plus a space
[x] ciw  change inner word   [x] caw
[x] yiw  yank inner word
[x] diW  inner WORD          [x] daW

      call(one, two) and more
[x] di(  inner parens        [x] da(  including them
[x] di)  same thing          [x] dib  the `b` alias
[x] ci(  change inside

      block { nested { deep } here } end
[x] di{  inner braces — nesting-aware
[x] da{                      [x] diB  the `B` alias
      list [ one, two ] and array [ three ]
[x] di[                      [x] da[
      generic <T, U> here
[x] di<                      [x] da<

      say "hello there" and 'goodbye' and `tick`
[x] di"  inner double quotes [x] da"
[x] di'                      [x] ci'
[x] di`

[x] dip  inner paragraph — the blank-line-delimited block
[x] dap

 Seeking, when the cursor is in no pair at all:
      the cursor here, the parens (over there) after it
[ ] di(  from the start of the row — it looks forward
[ ] di(  from *after* the parens — nothing, vi never seeks back
[ ] di{  from the row above a block, which is the useful one:
      fn example() {
          the whole body goes, from up on the signature
      }

    Quotes seek too, but only within the row — `di"` from the
    start of a row reaches a string later on it. Brackets seek
    across rows, quotes do not. That asymmetry is vi's.

    The braces stay put. When `{` ends its row and `}` owns its
    own, `di{` takes the body's rows and leaves the two braces
    where they were rather than pulling them onto one row. `ci{`
    then opens the row between them, which is what you want.

 Counts, which mean something different for each kind of object:
      outer { mid { deep } here } end
[ ] di{   the innermost braces, from the cursor on `deep`
[ ] 2di{  one level further out — a count climbs the nesting
[ ] 2da{  the same, taking the braces with it
[ ] 3di{  nothing to climb, so it rings the bell
[ ] d2i{  the count works either side of the operator

    A count is the nesting level you want, counted from where you
    stand — so from *outside* a block it runs the other way:
[ ] di{   from `outer`, the outermost braces ahead
[ ] 2di{  one level *in* — the braces around `deep`
[ ] 3di{  nothing nested that deep, so it rings
[ ] 2di{  on `outer {a} {b} end` — siblings are not a level

      one two three four
[ ] 2diw  a word and the space after it — whitespace is a run
[ ] 3diw  word, space, word
[ ] 2daw  two whole words
[ ] 9diw  stops at the row's end rather than joining rows

[ ] 2dip  a paragraph and the gap after it
[ ] 2dap  two paragraphs, each with its gap
[ ] 2di"  quotes do not nest, so the count is ignored
[ ] v2i{  a count extends a visual selection the same way

--------------------------------------------------------------
 9. SINGLE-KEY EDITS
--------------------------------------------------------------

      mangle THIS text however you like
[x] x    delete the character under the cursor
[x] <Del> same
[x] X    delete the character before it
[x] rZ   replace one character with Z
[ ] ~    swap case, and advance
[ ] 3~   swap three
[ ] J    join the next row onto this one
      join me
      to the row above
[ ] p    put after      [ ] P    put before
[ ] 2p   put twice

    Yank a row with `yy` then `p`: it should land on a new row.
    Yank a word with `yw` then `p`: it should land inline.
    That difference is the register's `linewise` flag — the
    status bar shows it as reg[line] or reg[char].

--------------------------------------------------------------
 10. UNDO
--------------------------------------------------------------

[ ] u     undo
[ ] <C-r> redo
[ ] U     undo every change on the current row
      break this row repeatedly then press U

    An insert session is ONE undo step: type `ihello<Esc>` then
    `u`, and all five characters go together — while the effect
    log shows five separate edits. Both granularities at once is
    the whole point.

[ ] U then u — the U itself is undoable
[ ] U twice — toggles back

    U after a multi-row change is a no-op, not a fallback to `u`.
    Try `ddU`. This diverges from vi deliberately.

--------------------------------------------------------------
 11. DOT REPEAT
--------------------------------------------------------------

[ ] .    repeat the last change
      repeat me and me and me and me
      ciwX<Esc> then w then . then w then .
[ ] 3.   repeat three times

    `.` after an insert session replays the typed text.
    Note: `3.` replays the whole command three times rather than
    substituting the count, which differs from vi.

--------------------------------------------------------------
 12. MACROS
--------------------------------------------------------------

[ ] qa   start recording into register a  (watch rec:@a)
[ ] q    stop recording
[ ] @a   play it back
[ ] 3@a  play it three times
      aaa one
      bbb two
      ccc three
      ddd four

    Try: qa~jq  then @a @a @a

--------------------------------------------------------------
 13. VISUAL MODE
--------------------------------------------------------------

      select some of this line and act on it
[ ] v then motions — the highlight follows
[ ] vd   delete the selection      [ ] vx  same
[ ] vc   change it                 [ ] vs  same
[ ] vy   yank it
[ ] viw  extend to inner word
[ ] va(  extend to a parens block
[ ] v then V — switch granularity mid-selection
[ ] V then j then d — delete whole rows
[ ] <Esc> cancels without acting

    The selection includes the character under the cursor, so
    what you see is what gets deleted.

 Case, on the selection rather than one character:
[ ] vwU  upper-case it     [ ] vwu  lower-case it
[ ] v$~  swap case across the selection
[ ] VjU  two whole rows
[ ] vwgU  the operator form works here too

    `u` in visual mode is a case change, not undo — that is vi, and
    it surprises people. `~` covers the whole selection instead of
    the single character it takes in normal mode.

--------------------------------------------------------------
 14. INSERT-MODE KEYS
--------------------------------------------------------------

[ ] typing inserts text
[ ] <CR>    split the row
[ ] <BS>    delete back
[ ] <BS> at column 0 — joins onto the previous row
[ ] <C-w>   delete the word before the cursor
[ ] <Tab>   inserts a real tab byte, shown as 8 columns
[ ] <Esc>   leave        [ ] <C-c>  also leaves
[ ] arrows move without leaving insert mode
      edit inside me

 <C-o> — one normal-mode command, then straight back to inserting:
[ ] <C-o>zz  centre the view without losing your place
[ ] <C-o>D   wipe the rest of the row and keep typing
[ ] <C-o>0   jump to the start and carry on there
[ ] <C-o>2dd  counts belong to the command, not the <C-o>
[ ] <C-o>w   one command only — the next w is text again
      come and edit somewhere in the middle of this row
[ ] <C-o>cw  a command that opens its own mode keeps it
[ ] <C-o><Esc>  leaves insert for good
[ ] <C-o> then a key that means nothing — bell, and you are
    handed back to insert rather than stranded in normal

    The status bar shows (INSERT) in parentheses while a <C-o> is
    in flight, and the cursor turns into a block: the grammar in
    force is normal mode's, but the session is still open.

    At the end of a row the caret stays past the last character,
    so A then <C-o>zz then typing continues where you were.

    <C-o> breaks the undo sequence, as vi does: type, <C-o>D, type
    more, then <Esc> — that is three `u` presses, not one.

--------------------------------------------------------------
 15. VIEWPORT
--------------------------------------------------------------

[ ] <C-d>  half page down   [ ] <C-u>  half page up
[ ] <C-f>  page down        [ ] <C-b>  page up
[ ] zz     centre the cursor row
[ ] zt     cursor row to the top
[ ] zb     cursor row to the bottom
[ ] H      top visible row  [ ] M middle  [ ] L bottom
[ ] 2H     second visible row      [ ] 2L second from bottom

    The core does not own the viewport. The harness reports its
    current top row and height whenever it draws; that lets page
    keys carry the cursor while it still emits a Scroll effect for
    the host to fulfil. Full pages retain two rows of overlap.

    Put the cursor on a long line, press $, then <C-d>: it stays at
    the end after moving. H, M and L go to the first non-blank of
    the reported visible rows, so scroll the file before trying H.

--------------------------------------------------------------
 16. PROMPTS — known gap
--------------------------------------------------------------

[ ] /   opens a prompt, and then cannot move the cursor
[ ] ?   likewise
[ ] n   [ ] N   report the same gap
[ ] :   opens a prompt the harness answers itself

    The core resolves these and emits an effect, but there is no
    API for a host to hand a match back. Confirm the prompt opens
    and the effect fires; the movement is genuinely not built yet.

--------------------------------------------------------------
 17. NOT BOUND — these should ring the bell
--------------------------------------------------------------

[ ] D   would be d$        [ ] C   would be c$
[ ] s   would be xi        [ ] S   would be cc
[ ] %   match a bracket
[ ] {   [ ] }   paragraph motions
[ ] <C-v>  visual block
[ ] "ayy  named registers — deliberately absent
[ ] ma    marks — deliberately absent

    Watch for `bell` in the log. Anything here that silently does
    something instead is a bug worth reporting.

--------------------------------------------------------------
 18. EDGES
--------------------------------------------------------------

    Multibyte — `x` must delete a whole grapheme, and the cursor
    must never land mid-character. The status bar shows the byte
    offset, so watch it jump by more than one.
      café niño — touché
      日本語のテキスト
      flags 🇦🇺 and family 👩‍👩‍👧‍👦 emoji

    Tabs — display width is the harness's job, not the core's.
    The byte column and the screen column will differ here.
      	one tab
      		two tabs
      a	middle	tab

    Empty rows — motions and operators over nothing.

[ ] dd on the blank row above
[ ] x on an empty row      [ ] J onto an empty row
[ ] $ on an empty row      [ ] diw on an empty row

    Trailing newline — go to the last row with G and try dd, o,
    and A. The file may or may not end in a newline; both should
    behave.

    The very end of the file is below. Nothing after this line.
